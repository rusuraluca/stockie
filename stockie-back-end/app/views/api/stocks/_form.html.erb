<%= form_with(model: api_stock) do |form| %>
  <% if api_stock.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_stock.errors.count, "error") %> prohibited this api_stock from being saved:</h2>

      <ul>
        <% api_stock.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
